repositories:
  - name: ingress-nginx
    url: https://kubernetes.github.io/ingress-nginx

releases:
  - name: nginx
    namespace: default
    chart: ingress-nginx/ingress-nginx
    values:
      - nginx-values.yaml
